[project]
name = "nfl-predictor"
version = "0.1.0"
description = "End-to-end NFL game outcome prediction project with rolling features, model comparison, and a Streamlit demo."
authors = [
    {name = "Noah",email = "nhymson9@icloud.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "nflreadpy (>=0.1.5,<0.2.0)",
    "polars (>=1.43.0,<2.0.0)",
    "pandas (>=3.0.5,<4.0.0)",
    "pyarrow (>=7.0.0,<25.0.0)",
    "numpy (>=2.5.1,<3.0.0)",
    "scikit-learn (>=1.9.0,<2.0.0)",
    "xgboost (>=3.3.0,<4.0.0)",
    "torch (>=2.13.0,<3.0.0)",
    "matplotlib (>=3.11.1,<4.0.0)",
    "streamlit (>=1.60.0,<2.0.0)",
    "notebook (>=7.6.1,<8.0.0)"
]

[tool.poetry]
package-mode = false

[tool.poetry.group.dev.dependencies]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
